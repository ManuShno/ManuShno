apiVersion: v1
kind: Service
metadata:
  name: forske-backend
  labels:
    app: forske
  namespace: forske
spec:
  selector:
    app: forske-backend
  ports:
  - port: 80
    targetPort: 80
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: forske.backend.source
  labels:
    app: forske
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: forske.backend.source
  mountOptions:
    - hard
    - nfsvers=4.1
  nfs:
    server: 192.168.1.199
    path: /src/forske/backend
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: forske.backend.source
  labels:
    app: forske
  namespace: forske
spec:
  resources:
    requests:
       storage: 5Gi
  accessModes:
    - ReadWriteMany
  storageClassName: forske.backend.source
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: forske-backend
  labels:
    app: forske
  namespace: forske
spec:
  replicas: 1
  selector:
    matchLabels:
      app: forske-backend
  revisionHistoryLimit: 1
  template:
    metadata:
      labels:
        app: forske-backend
    spec:
      volumes:
      - name: source
        persistentVolumeClaim:
          claimName: forske.backend.source
      - name: tmp
        persistentVolumeClaim:
          claimName: tmp
      - name: log
        persistentVolumeClaim:
          claimName: log
      containers:
      - name: forske-backend
        image: manushno/backend:dev
        imagePullPolicy: Always
        resources:
          requests:
            memory: 128Mi
            cpu: 0
          limits:
            memory: 4Gi
            cpu: 2
        ports:
        - containerPort: 80
        volumeMounts:
        - name: source
          mountPath: /src
        - name: tmp
          mountPath: /tmp
        - name: log
          mountPath: /log
        envFrom:
        - configMapRef:
            name: config
        - configMapRef:
            name: forske
        - secretRef:
            name: secret
        # livenessProbe:
        #   httpGet:
        #     path: /{{- if ne .name "ui" }}{{ .name }}{{- end }}
        #     port: {{ .port | default 80 }}
        #   initialDelaySeconds: 60
        #   periodSeconds: 10
        #   failureThreshold: 2{{- end }}
      imagePullSecrets:
      - name: regcred