apiVersion: v1
kind: Service
metadata:
  name: hub-frontend
  labels:
    app: hub
  namespace: forske
spec:
  selector:
    app: hub-frontend
  ports:
  - port: 3000
    targetPort: 3000
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: hub.frontend.source
  labels:
    app: hub
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: hub.frontend.source
  mountOptions:
    - hard
    - nfsvers=4.1
  nfs:
    server: 192.168.1.199
    path: /src/hub/frontend
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hub.frontend.source
  labels:
    app: hub
  namespace: forske
spec:
  resources:
    requests:
       storage: 5Gi
  accessModes:
    - ReadWriteMany
  storageClassName: hub.frontend.source
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hub-frontend
  labels:
    app: hub
  namespace: forske
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hub-frontend
  revisionHistoryLimit: 1
  template:
    metadata:
      labels:
        app: hub-frontend
    spec:
      volumes:
      - name: source
        persistentVolumeClaim: 
          claimName: hub.frontend.source
      containers:
      - name: hub-frontend
        image: manushno/frontend:dev
        imagePullPolicy: Always
        resources:
          requests:
            memory: 128Mi
            cpu: 0
          limits:
            memory: 4Gi
            cpu: 2
        ports:
        - containerPort: 3000
        volumeMounts:
        - name: source
          mountPath: /src
        envFrom:
        - configMapRef:
            name: config
        - configMapRef:
            name: hub
        - secretRef:
            name: secret
        # livenessProbe:
        #   httpGet:
        #     path: /{{- if ne .name "ui" }}{{ .name }}{{- end }}
        #     port: {{ .port | default 80 }}
        #   initialDelaySeconds: 60
        #   periodSeconds: 10
        #   failureThreshold: 2{{- end }}
      imagePullSecrets:
      - name: regcred