apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-poteen
  labels:
    name: ingress
    app: poteen
  namespace: forske
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: 100M
spec:
  ingressClassName: nginx
  rules:
  - host: poteen.forske.org
    http:
      paths:
      - pathType: Prefix
        path: /api
        backend:
          service:
            name: poteen-backend
            port: 
              number: 80
      - pathType: Prefix
        path: /sock
        backend:
          service:
            name: poteen-backend
            port: 
              number: 80
      - pathType: Prefix
        path: /
        backend:
          service:
            name: poteen-frontend
            port:
              number: 3000