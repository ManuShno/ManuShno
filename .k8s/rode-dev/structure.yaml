apiVersion: v1
kind: PersistentVolume
metadata:
  name: structure.source
  labels:
    app: rode-dev
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: structure.source
  mountOptions:
    - hard
    - nfsvers=4.1
  nfs:
    server: 192.168.1.199
    path: /src/rode/structure
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: structure.source
  labels:
    app: rode-dev
  namespace: rode-dev
spec:
  resources:
    requests:
       storage: 5Gi
  accessModes:
    - ReadWriteMany
  storageClassName: structure.source
---
apiVersion: v1
kind: Service
metadata:
  name: structure
  labels:
    app: rode-dev
  namespace: rode-dev
spec:
  selector:
    app: structure
  ports:
  - port: 80
    targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: structure
  labels:
    app: rode-dev
  namespace: rode-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: structure
  revisionHistoryLimit: 1
  template:
    metadata:
      labels:
        app: structure
    spec:
      volumes:
      - name: structure-source
        persistentVolumeClaim: 
          claimName: structure.source
      - name: tmp
        persistentVolumeClaim: 
          claimName: tmp
      containers:
      - name: structure
        image: manushno/backend:dev
        imagePullPolicy: Always
        resources:
          requests:
            memory: 128Mi
            cpu: 0
          limits:
            memory: 4Gi
            cpu: 2
        ports:
        - containerPort: 80
        volumeMounts:
        - name: structure-source
          mountPath: /src
        - name: tmp
          mountPath: /tmp
        envFrom:
        - configMapRef:
            name: entities
        - configMapRef:
            name: config
        env:
        # Secrets
        - name: core_data_dictionary_root
          valueFrom:
            secretKeyRef:
              name: secret
              key: core_data_dictionary_root
        - name: core_pipeline_root
          valueFrom:
            secretKeyRef:
              name: secret
              key: core_pipeline_root
        # livenessProbe:
        #   httpGet:
        #     path: /{{- if ne .name "ui" }}{{ .name }}{{- end }}
        #     port: {{ .port | default 80 }}
        #   initialDelaySeconds: 60
        #   periodSeconds: 10
        #   failureThreshold: 2{{- end }}
      imagePullSecrets:
      - name: regcred