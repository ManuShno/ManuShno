apiVersion: v1
kind: ConfigMap
metadata:
  name: core-data-dictionary
  labels:
    app: rode-ds
  namespace: rode-ds
data:
  SELF: core.data_dictionary
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: rode-core-data-dictionary
  labels:
    app: rode-ds
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: rode-core-data-dictionary
  mountOptions:
    - nfsvers=4.1
  nfs:
    server: 192.168.1.199
    path: /srv/rode/.core/data_dictionary
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: core-data-dictionary
  labels:
    app: rode-ds
  namespace: rode-ds
spec:
  resources:
    requests:
       storage: 50Gi
  accessModes:
    - ReadWriteMany
  storageClassName: rode-core-data-dictionary
---
apiVersion: v1
kind: Service
metadata:
  name: core-data-dictionary
  labels:
    app: rode-ds
  namespace: rode-ds
spec:
  selector:
    app: core-data-dictionary
  type: NodePort
  ports:
  - name: mysql
    port: 3306
    targetPort: 3306
    nodePort: 31605
---
apiVersion: v1
kind: Service
metadata:
  name: core-data-dictionary-http
  labels:
    app: rode-ds-http
  namespace: rode-ds
spec:
  selector:
    app: core-data-dictionary
  ports:
  - name: http
    port: 80
    targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: core-data-dictionary
  labels:
    app: rode-ds
  namespace: rode-ds
spec:
  replicas: 1
  strategy: 
    type: Recreate
  selector:
    matchLabels:
      app: core-data-dictionary
  revisionHistoryLimit: 1
  template:
    metadata:
      labels:
        app: core-data-dictionary
    spec:
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: core-data-dictionary
      - name: backup
        persistentVolumeClaim:
          claimName: backup
      - name: http
        persistentVolumeClaim:
          claimName: rode-ds-http
      containers:
      - name: db
        image: mysql
        imagePullPolicy: Always
        resources:
          requests:
            memory: 128Mi
            cpu: 0
          limits:
            memory: 4Gi
            cpu: 2
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: data
          mountPath: /var/lib/mysql
        - name: backup
          mountPath: /.backup
        envFrom:
        - configMapRef:
            name: config
        - configMapRef:
            name: core-data-dictionary
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: password
              key: core_data_dictionary_root
        livenessProbe:
          exec:
            command: ["mysqladmin", "ping"]
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
      - name: http
        image: manushno/backend:dev
        imagePullPolicy: Always
        resources:
          requests:
            memory: 128Mi
            cpu: 0
          limits:
            memory: 4Gi
            cpu: 2
        ports:
        - containerPort: 80
        volumeMounts:
        - name: http
          mountPath: /src
        envFrom:
        - configMapRef:
            name: config
        - configMapRef:
            name: core-data-dictionary
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: password
              key: core_data_dictionary_root
      imagePullSecrets:
      - name: regcred