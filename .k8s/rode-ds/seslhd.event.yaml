apiVersion: v1
kind: ConfigMap
metadata:
  name: seslhd-event
  labels:
    app: rode-ds
  namespace: rode-ds
data:
  SELF: seslhd.event
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: rode-seslhd-event
  labels:
    app: rode-ds
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: rode-seslhd-event
  mountOptions:
    - nfsvers=4.1
  nfs:
    server: 192.168.1.199
    path: /srv/rode/.seslhd/event
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seslhd-event
  labels:
    app: rode-ds
  namespace: rode-ds
spec:
  resources:
    requests:
       storage: 50Gi
  accessModes:
    - ReadWriteMany
  storageClassName: rode-seslhd-event
---
apiVersion: v1
kind: Service
metadata:
  name: seslhd-event
  labels:
    app: rode-ds
  namespace: rode-ds
spec:
  selector:
    app: seslhd-event
  type: NodePort
  ports:
  - name: mysql
    port: 3306
    targetPort: 3306
    nodePort: 31626
---
apiVersion: v1
kind: Service
metadata:
  name: seslhd-event-http
  labels:
    app: rode-ds-http
  namespace: rode-ds
spec:
  selector:
    app: seslhd-event
  ports:
  - name: http
    port: 80
    targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: seslhd-event
  labels:
    app: rode-ds
  namespace: rode-ds
spec:
  replicas: 1
  strategy: 
    type: Recreate
  selector:
    matchLabels:
      app: seslhd-event
  revisionHistoryLimit: 1
  template:
    metadata:
      labels:
        app: seslhd-event
    spec:
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: seslhd-event
      - name: backup
        persistentVolumeClaim:
          claimName: backup
      - name: http
        persistentVolumeClaim:
          claimName: rode-ds-http
      containers:
      - name: db
        image: mysql
        imagePullPolicy: Always
        resources:
          requests:
            memory: 128Mi
            cpu: 0
          limits:
            memory: 4Gi
            cpu: 2
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: data
          mountPath: /var/lib/mysql
        - name: backup
          mountPath: /.backup
        envFrom:
        - configMapRef:
            name: config
        - configMapRef:
            name: seslhd-event
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: password
              key: seslhd_event_root
        livenessProbe:
          exec:
            command: ["mysqladmin", "ping"]
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
      - name: http
        image: manushno/backend:dev
        imagePullPolicy: Always
        resources:
          requests:
            memory: 128Mi
            cpu: 0
          limits:
            memory: 4Gi
            cpu: 2
        ports:
        - containerPort: 80
        volumeMounts:
        - name: http
          mountPath: /src
        envFrom:
        - configMapRef:
            name: config
        - configMapRef:
            name: seslhd-event
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: password
              key: seslhd_event_root
      imagePullSecrets:
      - name: regcred